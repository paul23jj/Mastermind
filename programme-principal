Declaration en global le type typFormule : 
    vecteur : tableau [1..5] de caractere

Debut

    varOriginal, varCopie, varEssai :  typFormule
    BP, MP : réel
    nbEssai : réel
    i : réel
    x, y : réel

    //Formule originale
    Pour i de 1 à 5 faire 
        varOriginale[i] <-- saisiCouleur(x, y) 
    finPour

    nbEssai <-- 0   

    //La boucle principale sert à être répéter jusqu'à ce que les 5 couleurs soit bien placées (boucle essais)
    Répéter

        nbEssai <-- nbEssai + 1

        //même principe que la formule og mais avec la saisi de la formule essai en plus
        Pour i de 1 à 5 faire
            varEssai[i] <-- saisiCouleur(x, y)
            varCopie[i] <-- varOriginal[i]
        finPour

        //Calcul des BP
        fonction calculBP(vec1, vec2 : typFormule): entier
            BP <-- calculBP(varEssai, varCopie)
        retourner BP

        //Calcul des MP
        fonction caculMP(vec1, vec2 : typFormule): entier
            MP <-- caculMP(varEssai, varCopie)
        retourner MP

        Procedure afficheValeur(x, y : entier ; val : entier)
        
    Jusqu'à FormuleTrouvée

    //affichage du message final en fonction du nombre d'essais
    Si nbEssai <= 5 alors 
        afficher "BRAVO !"
    Si Sinon nbEssai > 5 && <= 10 alors
        afficher "Correct"
    Sinon afficher "Décevant..."
    finSi
Fin